<%if (contacts.length>0){%>
<h2 class="mt-2">لیست مخاطبین من :) </h2>

<div class="table-responsive">
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th>#</th>
        <th>نام و نام خانوادگی</th>
        <th>تاریخ ثبت</th>
        <th>شماره تماس</th>
        <th>آدرس ایمیل</th>
        <th>تنظیمات</th>
        <th>تعداد کل مخاطبین:<%=numberOfContacts  %> </th>
      </tr>
    </thead>
    <tbody>
      <% for (let contact of contacts){%>
      <tr>
        <td>*</td>
        <td><a href="/contacts/<%=contact.id%>"></a><%=contact.fullname%></td>


        <td><%=formatDate(contact.createdAt)%></td>
        <td><%=contact.phone%></td>
        <td><%=contact.email%></td>
        
        <td>
          <a
            href="/dashboard/edit-contact/<%=contact._id%>"
            class="btn btn-sm btn-primary"
            >ویرایش</a
          >
          <a
            href="/dashboard/delete-contact/<%=contact._id%>"
            class="btn btn-sm btn-danger"
            >حذف</a
          >
        </td>
        <td></td>
      </tr>
      <%}%>
    </tbody>
  </table>
<!-- pagination -->
</div>
<%}else{%>
<div class="text-center">
  <h3 class="mt-2">
    در حال حاضر هیچ پستی توسط شما در پایگاه داده ثبت نشده است
  </h3>
</div>
<%}%>
